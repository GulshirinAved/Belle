part of '../theme_config.dart';

@immutable
class AppDimensions {
  const AppDimensions._();

  /// [paddingExtraSmall] is `2.0`
  static const paddingExtraSmall = 2.0;

  /// [paddingSmall] is `5.0`
  static const paddingSmall = 5.0;

  /// [paddingMedium] is `10.0`
  static const paddingMedium = 10.0;

  /// [paddingLarge] is `14.0`
  static const paddingLarge = 14.0;

  /// [paddingExtraLarge] is `20.0`
  static const paddingExtraLarge = 20.0;

  /// [paddingGiant] is `25.0`
  static const paddingGiant = 25.0;

  /// [paddingExtraGiant] is `30.0`
  static const paddingExtraGiant = 30.0;

  /// [radiusExtraSmall] is `2.0`
  static const radiusExtraSmall = 2.0;

  /// [radiusSmall] is `6.0`
  static const radiusSmall = 6.0;

  /// [radiusMediumSmall] is `9.0`
  static const radiusMediumSmall = 9.0;

  /// [radiusMedium] is `12.0`
  static const radiusMedium = 12.0;

  /// [radiusLarge] is `15.0`
  static const radiusLarge = 15.0;

  /// [radiusExtraLarge] is `20.0`
  static const radiusExtraLarge = 20.0;

  /// [radiusGiant] is `25.0`
  static const radiusGiant = 25.0;

  /// [buttonWidthDefault] is `320.0`
  static const buttonWidthDefault = 320.0;

  /// [buttonHeightDefault] is `50.0`
  static const buttonHeightDefault = 50.0;

  /// [avatarPictureHeight] is `400.0`
  static const avatarPictureHeight = 400.0;

  /// [avatarPictureWidth] is `400.0`
  static const avatarPictureWidth = 400.0;
}
