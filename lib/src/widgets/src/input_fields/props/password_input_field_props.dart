import 'package:flutter/material.dart';

class PasswordInputFieldProps {
  final String? labelText;
  final TextEditingController? textEditingController;

  const PasswordInputFieldProps({
    this.labelText,
    this.textEditingController,
  });
}
