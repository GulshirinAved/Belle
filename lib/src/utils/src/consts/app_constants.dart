import 'package:flutter/material.dart';

@immutable
class AppConstants {
  const AppConstants._();

  static const requiresAuthToken = 'requiresAuthToken';
  // static const requiresRefreshToken = 'requiresRefreshToken';
}
